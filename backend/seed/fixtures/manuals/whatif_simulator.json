{
  "module_id": "whatif_simulator",
  "module_name": "What-If Simulator",
  "sections": [
    {
      "title": "Overview",
      "body": "The What-If Simulator lets Controllers, Executives, and CC Owners model hypothetical portfolio scenarios without affecting live data. It operates in three phases: Scenario Manager (browse / create), Scenario Workspace (build and analyze), and Comparison View (side-by-side analysis). Project Leads cannot author scenarios in v5 per `[E-06c]` but can apply published scenarios to their own forecast (see Apply to Forecast)."
    },
    {
      "title": "Scenario Manager",
      "body": "The landing view shows your saved scenarios and published scenarios from others. Each scenario card shows its name, author, status (private / published), tags, and headline impact. Create a new scenario from scratch or clone an existing one. Delete scenarios you own. Publish scenarios to make them visible to other users (subject to Tier 3 gating — see below). Archive scenarios that have served their purpose; archived scenarios are hidden by default but visible under the Show archived filter."
    },
    {
      "title": "Scenario Workspace",
      "body": "The workspace has a split layout: an action panel on the left and an impact dashboard on the right. Add actions to model changes, and the impact dashboard updates immediately with the recalculated results. Nine dimension tiles populate after recalculation: Financial / Ranking / Capacity / People / Outsourcing / Investment Mix / Running Cost / Cost Allocation / Change Summary. The header shows the last-recalculated indicator (e.g. 'Recalculated 36d ago') and a Stale flag if the anchor ForecastVersion has been superseded — use the Rebase action in the ⋯ menu to re-anchor."
    },
    {
      "title": "Twelve Action Levers (v5)",
      "body": "Per `[B-AC-02]`:\n1. Delay project start (forecast_grid · Tier 1)\n2. Reduce project hours (forecast_grid · Tier 1)\n3. Remove project / cancel (forecast_grid · Tier 1)\n4. Accelerate project (forecast_grid · Tier 1)\n5. Add scope / new line items (forecast_grid · Tier 1)\n6. Sourcing mix int/ext (people · Tier 2)\n7. Rate table change (rate_table · Tier 2)\n8. Tech Navigator profile edit (tech_navigator · Tier 1)\n9. Capacity allocation change (forecast_grid · Tier 2)\n10. Distribution edge — Stage 1 (cost_allocation · Tier 3)\n11. BTC profile line shift — Stage 2 (cost_allocation · Tier 3)\n12. Pipeline stage / DoI / milestone shift (pipeline_stage · Tier 1-2)\n\nLevers 10 and 11 (cost_allocation) flag `tier3_content_flag=true` and require Tier 3 visibility for publish + promote per `[F-AC-01]`."
    },
    {
      "title": "Lever 12: BTC sandbox + impact preview + promote",
      "body": "Lever 12 (and the related Lever 10 distribution mutation) is the F-cluster's flagship sandbox per `[B-LV-12]`. Edits to BTC profile lines and Stage 1 distribution edges do NOT touch canonical state — they're stored two ways:\n- Stage 1 edges fork into `Distribution.version='scenario-{id}'` rows on first mutation per `[F-S1-04]`; live `version='forecast'` rows are never touched.\n- Stage 2 BTC line and `to_business_pct` mutations live as `ScenarioAction` overlays (live `BTCProfile` / `BTCProfileLine` rows untouched) and are applied at impact-calc time.\n\n**Sandbox UI**: in the scenario workspace, navigate to the **Cost Allocation Sandbox** surface (Bulk Actions → Cost Allocation). The same Charging editors mount here in sandbox mode — UM refresh and mode-switch buttons are hidden by design (canonical-only operations); a blue 'Sandbox edit' chip indicates the mode.\n\n**Impact preview**: the right-side Cost Allocation dimension tile shows headline numbers (e.g. 'Rebalances ~€228k of annual Stage-2 charge from DE legal entity to PL+CZ legal entities'). The embedded RollupView mounts in compact + scenario-version mode for visual diff.\n\n**Promote**: the ⋯ menu's Promote action materialises overlays onto canonical rows. Permission per `[F-AC-01]`: controllers always allowed; other roles need an explicit `RolePermissionGrant`. Audit trail: each Promote click creates a `ScenarioPromotion` row with `routing_summary_json` listing per-action routing, status, and target."
    },
    {
      "title": "Tier 3 Visibility Gating",
      "body": "Per `[B-SL-03]`, scenarios containing Lever 10 or Lever 11 actions auto-set `tier3_content_flag=true`. Three visibility levels:\n- `private` — author only\n- `tier3_only` — Tier 3 users (those with `tier3_flag=true` on their User row)\n- `all_users` — anyone authenticated\n\n**Publish gating**: trying to publish a Tier 3 scenario without `tier3_flag` is blocked with 'Tier 3 publish requires elevated permissions'. Promoting a Tier 3 scenario requires both `tier3_flag=true` AND a `RolePermissionGrant` for the relevant `entity_type` (`btc_profile` or `distribution`). Controllers default-have these grants; other roles need explicit admin-issued grants per `[F-AC-01]`.\n\n**Example**: 'MDH BTC Rebalance — DE/PL/CZ' is a Tier 3 scenario authored by Anna Meier. Publishing it as `tier3_only` makes it visible to other Tier 3 users; promoting it materialises BTC line shifts onto the live `off-mdh` profile."
    },
    {
      "title": "Apply to Forecast (PL flow)",
      "body": "When a published scenario's actions touch projects a PL owns, the PL can pre-populate their next forecast cycle with those diffs per `[B-PR-05]`:\n1. From `/simulator`, locate a published scenario whose tags / description suggest it touches your projects.\n2. Click the row — workspace opens in read-only mode (PL can't edit a non-owned scenario).\n3. Click **Apply to forecast** in the page header → modal lists which of your projects the scenario touches.\n4. Click **Confirm apply** → `ScenarioApplyToForecastEvent` row created; the PL's next forecast cycle has the diffs pre-loaded with `Forecast.is_provisional=true` for cells in the outer zone (beyond the granularity boundary per `[C-FG-07]`).\n\nThe PL still drives the forecast cycle submission afterwards — applying a scenario does not auto-submit a forecast. Provisional cells get a visible badge in the Workbench grid until the PL accepts them as part of the cycle."
    },
    {
      "title": "Actions",
      "body": "Actions can target individual projects or the entire portfolio.\n\n**Project-scope actions:** Remove Project, Reduce/Adjust Budget, Increase Budget, Delay Project, Accelerate Project, Pause Project, Change Allocation (add, modify, or remove resource assignments), Cut Consulting, and Adjust External Cost.\n\n**Portfolio-scope actions:** Across-the-Board Cut (percentage reduction on all projects), Cut by LoB (target a specific organizational entity), Cut by Type (target CapEx or OpEx), Freeze New Starts (prevent planned projects from beginning), Cap Cost Category (limit a specific cost type), and Rate Escalation (model hourly rate increases).\n\n**Tech Navigator actions** (Lever 8): Edit a project's TN sub-criteria within the scenario; the composite recomputes against the scenario weights, allowing the user to model 'what if Project X had a different scoring profile'.\n\nEach action specifies parameters (e.g., delay by N months, reduce by X%). Every action returns the full recalculated scenario state."
    },
    {
      "title": "Impact Dashboard (9 dimensions)",
      "body": "Shows the cascading effects of all applied actions across nine dimensions:\n- **Financial** — total budget delta, capex/opex split, savings target tracking\n- **Ranking** — Backlog cutoff impact, cutoff line shift\n- **Capacity** — FTE delta per CC, utilisation shifts\n- **People** — over/under-allocated count by role\n- **Outsourcing** — internal vs external mix\n- **Investment Mix** — by hierarchy node / by type / by RAG band\n- **Running Cost** — Run Portfolio impact (DoI 5 entities)\n- **Cost Allocation** — Stage 1 + Stage 2 deltas (Lever 10/11 only)\n- **Change Summary** — flat list of every action with categorisation\n\nDrill down into any metric to see project-level detail."
    },
    {
      "title": "Comparison View",
      "body": "Select 2–5 scenarios to compare side-by-side. The comparison shows key metrics (budget, timeline, capacity, RAG changes) for each scenario in parallel columns. The first column always shows the **Current State** as a baseline for comparison. Click any scenario card in the comparison to drill into its full workspace."
    },
    {
      "title": "AI Advisor",
      "body": "The AI Advisor panel (top-right of the workspace) lets you describe a goal in natural language — for example, 'Find 2M in savings' or 'Get all teams below 95% utilization'. The system matches your goal to pre-computed solution paths and presents 2–3 options with different trade-offs. Select a path to apply its constituent actions to your scenario.\n\n**Seeded goals (5 in v5)**:\n- *Find approximately 2M in savings* (Conservative / Moderate / Aggressive)\n- *Get all teams below 95% utilization* (Redistribute / Defer)\n- *Prioritize Rail Systems investments* (Accelerate / Balanced)\n- *Eliminate Red RAG projects* (Fund It / Reduce Scope)\n- *Rebalance Master Data Hub BTC across DE/PL/CZ* (DE→PL/CZ split / DE→broader EMEA) — v5 lever-12 demo\n\nIf no pattern matches, the advisor returns a friendly hint listing the available goal areas. With `ANTHROPIC_API_KEY` set in Planning Parameters, the advisor can also engage in dynamic Claude-powered conversations."
    },
    {
      "title": "CC-Owner-scoped scenarios",
      "body": "Per `[F-AC-01]`, when a CC Owner (e.g. Thomas Brenner) creates a scenario, the scenario is automatically scoped to `cc_owner_scope_cc_id=<their CC>`. The scenario is only visible to that CC's owners + controllers; published CC-scoped scenarios surface under the recipient's Published list with a CC chip. CC-scoped scenarios cannot be promoted by their author — promotion still requires the controller's permission grant for the relevant entity type."
    },
    {
      "title": "Important Notes",
      "body": "Scenarios are sandboxes — they never affect live portfolio data unless promoted (Lever 10/11) or applied-to-forecast (PL flow). All impact calculations are performed server-side. Pre-built scenarios use pre-computed snapshots for instant loading. The AI Advisor uses pattern matching by default and Claude when API key is configured. All charts and visualizations support both light and dark mode themes. Scenario delete cleanup removes scenario-version Distribution rows and BTC overlays per `services/scenario_lever12.cleanup_lever12_state`."
    }
  ]
}
