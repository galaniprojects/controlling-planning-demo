/**
 * v5 B2 — Target-setter catalogue actions (2).
 *
 * Each component is a thin wrapper around `ActionForm` bound to the
 * matching `ActionDefinition` from `catalogueDef.ts`. Both are flagged
 * `informational` and route to `no_route` at Promote time.
 */

export { OutsourcingTargetAction } from './OutsourcingTarget';
export { InvestmentMixTargetAction } from './InvestmentMixTarget';
