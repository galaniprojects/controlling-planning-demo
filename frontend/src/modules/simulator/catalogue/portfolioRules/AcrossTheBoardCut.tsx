/**
 * v5 B2 — Portfolio-rule catalogue action: AcrossTheBoardCut.
 *
 * Thin wrapper around the shared ActionForm. Definition lives in
 * `catalogueDef.ts` (id: `across-the-board-cut`).
 */

import { makeCatalogueWrapper } from '../wrapper';

export const AcrossTheBoardCutAction = makeCatalogueWrapper('across-the-board-cut');
