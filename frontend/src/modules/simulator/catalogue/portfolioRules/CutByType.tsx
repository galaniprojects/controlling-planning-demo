/**
 * v5 B2 — Portfolio-rule catalogue action: CutByType.
 *
 * Thin wrapper around the shared ActionForm. Definition lives in
 * `catalogueDef.ts` (id: `cut-by-type`).
 */

import { makeCatalogueWrapper } from '../wrapper';

export const CutByTypeAction = makeCatalogueWrapper('cut-by-type');
