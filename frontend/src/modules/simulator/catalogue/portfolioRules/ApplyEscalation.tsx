/**
 * v5 B2 — Portfolio-rule catalogue action: ApplyEscalation.
 *
 * Thin wrapper around the shared ActionForm. Definition lives in
 * `catalogueDef.ts` (id: `apply-escalation`).
 */

import { makeCatalogueWrapper } from '../wrapper';

export const ApplyEscalationAction = makeCatalogueWrapper('apply-escalation');
