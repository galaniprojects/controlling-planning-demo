/**
 * v5 B2 — Portfolio-rule catalogue action: AdjustRateTable.
 *
 * Thin wrapper around the shared ActionForm. Definition lives in
 * `catalogueDef.ts` (id: `adjust-rate-table`).
 */

import { makeCatalogueWrapper } from '../wrapper';

export const AdjustRateTableAction = makeCatalogueWrapper('adjust-rate-table');
