/**
 * v5 B2 — Portfolio-rule catalogue action: CutByTransformation.
 *
 * Thin wrapper around the shared ActionForm. Definition lives in
 * `catalogueDef.ts` (id: `cut-by-transformation`).
 */

import { makeCatalogueWrapper } from '../wrapper';

export const CutByTransformationAction = makeCatalogueWrapper('cut-by-transformation');
