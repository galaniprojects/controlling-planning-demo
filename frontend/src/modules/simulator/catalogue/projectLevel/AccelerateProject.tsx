/**
 * v5 B2 — Project-level catalogue action: AccelerateProject.
 *
 * Thin wrapper around the shared ActionForm. Definition lives in
 * `catalogueDef.ts` (id: `accelerate-project`).
 */

import { makeCatalogueWrapper } from '../wrapper';

export const AccelerateProjectAction = makeCatalogueWrapper('accelerate-project');
