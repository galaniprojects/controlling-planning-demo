/**
 * v5 B2 — Project-level catalogue action: ScaleBudget.
 *
 * Thin wrapper around the shared ActionForm. Definition lives in
 * `catalogueDef.ts` (id: `scale-budget`).
 */

import { makeCatalogueWrapper } from '../wrapper';

export const ScaleBudgetAction = makeCatalogueWrapper('scale-budget');
