/**
 * v5 B2 — Project-level catalogue action: AdjustVendorContract.
 *
 * Thin wrapper around the shared ActionForm. Definition lives in
 * `catalogueDef.ts` (id: `adjust-vendor-contract`).
 */

import { makeCatalogueWrapper } from '../wrapper';

export const AdjustVendorContractAction = makeCatalogueWrapper('adjust-vendor-contract');
