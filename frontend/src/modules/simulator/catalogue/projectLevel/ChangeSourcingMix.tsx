/**
 * v5 B2 — Project-level catalogue action: ChangeSourcingMix.
 *
 * Thin wrapper around the shared ActionForm. Definition lives in
 * `catalogueDef.ts` (id: `change-sourcing-mix`).
 */

import { makeCatalogueWrapper } from '../wrapper';

export const ChangeSourcingMixAction = makeCatalogueWrapper('change-sourcing-mix');
